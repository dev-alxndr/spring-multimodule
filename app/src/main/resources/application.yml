server:
  port: 8088

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mm
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false